/**
 * @file
 * Algorand behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.algorandAlgorand = {
    attach (context, settings) {

      console.log('It works!');

    }
  };

} (Drupal));
